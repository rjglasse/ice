# Improvements backlog

Two kinds of change. **Tooling** changes make the weekly run easier and do not alter what
students see. **Treatment** changes alter the nudges, the scoring, or the guide; they must be
dated here because they can move the 2026 numbers. Status: done / proposed / decision needed.

| ID | Kind | Status | Change | Source |
|---|---|---|---|---|
| IMP-1 | tooling | done 2026-09-05 | `ice.py` runs the whole week (clone, extract, analyse, compare, post) with a dry run by default. | last year "very manual"; survey T7 (quicker feedback) |
| IMP-2 | tooling | done 2026-09-05 | `discover.py` infers teachers (org admins + template commit authors), students (repo owners), deadline and exercise count (README majority vote). Manual JSON wins. | config pain point |
| IMP-3 | measurement | done 2026-03-09, handled 2026-09-05 | Closing-keyword regex now counts bare `fix #N` / `close #N` / `resolve #N` (GitHub accepts them). During 2025 tasks 1-8 these were not counted. `compare.py` recomputes the 2025 baseline with the current regex. | survey T3: "It didn't recognize my closing keywords" |
| IMP-4 | treatment | decision needed | Traceability nudge (issue references per commit) is disabled, as in 2025. Keep disabled for comparability, or re-enable. | code comment in `nudges.py` |
| IMP-5 | treatment | done 2026-09-05 (before any 2026 posting) | The feedback footer now links https://gits-15.sys.kth.se/inda-26/course-instructions/blob/master/ice-guide.md instead of "see ice-guide.md in the ICE repository". The guide must be kept identical in `ice-guide.md` here, in inda-26/course-instructions and in the inda-master template (copied each year). | survey T5 (onboarding clarity) |
| IMP-6 | treatment | proposed | Ceiling effect: students who reach Master stop reading. Options: a streak line ("Master 3 weeks running"), a stretch criterion, or a shorter message for repeat Masters. | survey T1 |
| IMP-7 | measurement | proposed | Quality signals beyond counts: share of commits with a message beyond `Fixes #N`, share of issues with a customised title, commits per exercise. Measure silently in 2026 first; show later. | survey T2 ("just counting") |
| IMP-8 | treatment | proposed | Theoretical (non-code) exercises count as issues that need commits. Allow `non_code_exercises` per task in the cohort JSON to relax the commit expectation. | survey T4 |
| IMP-9 | coverage | decided 2026-09-05 | Plus-track students stay excluded, as in 2025. Repositories with no commits (`inactive.csv`, reported per task in `compare.md`) are the week-on-week signal for drop-off; make sure it is collected every week (ice.py does). | survey T3/T6 |
| IMP-10 | tooling | done 2026-09-05 | Teacher matching: discovered names match exactly; the shared list keeps substring matching. "Ric" removed (would match any author containing "ric"). | bug in old `context.py` (missing comma) |
| IMP-11 | treatment | done 2026-09-05 | Scoring code removed from the end of `ice-guide.md` to limit gaming; the five pillars and category names stay. Copy the updated guide to inda-26/course-instructions and inda-master. | ChatGPT critique notes; paper threats |
| IMP-12 | treatment | done 2026-09-05 (option b implemented as "Where you are") | Cohort positioning stays (classic social-norm nudge, part of the treatment). Candidate reformulations, to decide before a mid-course change: (a) descriptive norm only ("most students created an issue per exercise") without a ranked ladder; (b) show the student's own movement since last week next to the cohort share; (c) shorter list: only the student's category and the share above/below; (d) frame the top category as the norm rather than an elite ("63% of the class are here"). Any change dates from the task it first appears in. | survey T6 |
| IMP-13 | tooling | done 2026-09-05 | `data/` is tracked in git (private working copy only, see IMP-24); `data/2025` moved under a year folder. Five 2025 CSVs had been hand-aligned in March 2026, which broke the quoting of issue titles containing a comma (119 rows in task-1, 227 in task-2) and shifted their `state` column. task-1 `issues.csv` restored from the September 2025 commit, task-2 repaired by re-joining the split title. After the repair the stored 2025 `effort.csv` is reproduced exactly from raw data for every task except the closing-keyword rows of IMP-3. | repo hygiene |
| IMP-14 | tooling | done 2026-09-05 | Repo discovery matches the `-task-N` suffix exactly (`task-1` no longer also matches `task-10`..`task-18`). Verified: no 2025 file was contaminated. | code review |
| IMP-15 | tooling | done 2026-09-05 | `issues.csv` gains a `closedAt` column (enables "closed before deadline" later). Old readers unaffected. | enables C3 refinements |
| IMP-16 | tooling | done 2026-09-05 | Historical cohorts 2020-2024 (tasks 1-9, commits + issues) imported to `data/<year>/` with `cohorts/<year>.json` from the old context files; `history.py` reproduces the paper's commits-per-student-by-year comparison with 2026 added, and `compare.py --baseline YEAR` works for any year. | study-2026.md C1 |
| IMP-17 | tooling | superseded by IMP-21 (2026-09-05) | Post-deadline timing: commits after the deadline are now kept in `commits.csv` and flagged `after_deadline`, so the share of late work is visible without a separate view. | idea |
| IMP-18 | treatment | done 2026-09-05, applies to all 2026 tasks (task-1 posted with v2 the same day) | Nudge model v2 (`nudge_model` in the cohort JSON). Planning is judged on the plan the student chose, detected from issue titles against the README's link titles (`plan.py`): default complete, default partial, own, mixed, none. An own or mixed plan that is used (referenced from a commit or closed) counts as a full plan; the 50% underplanning penalty applies only to a partially used default plan or no plan; warmup issues are not counted as plan. Breadth condition (same day): a used plan is full only with ≥2 issues or coverage of half the exercises; a single used issue for one exercise scores 0.6 on planning (lands in Strong Practitioner) and the planning line says the plan covered a small part of the task. Weights and categories unchanged. The 2025 model still runs (`score_v1`, `classification_v1` in nudges.csv) and `compare.py` uses it, so cross-year tables are unaffected. On task-1 2026, 20 of 160 students change category (14 up, 6 down; the downs are partial default plans where the warmup issue no longer counts). | user: 2025 scoring "did not go down well"; acknowledge students where they are |
| IMP-19 | tooling | proposed | Guide text: describe planning as "a plan you use", with the default links as one option, matching the v2 wording. | IMP-18 |
| IMP-20 | analysis | done 2026-09-05 | `plan.py --trend` reports plan styles per task and week-to-week transitions (`reports/plan-styles-2025.md`), to see whether the 2025 feedback pushed own/mixed planners onto the default links. | user question on agency |
| IMP-21 | measurement + treatment | done 2026-09-05 | Cutoff semantics. Feedback counts every commit present at clone time (Saturday); commits after the task deadline are kept in `commits.csv` and flagged `after_deadline`. `compare.py` recomputes both years with the flagged commits excluded, so the study view keeps the 2025 cutoff (end of exercise day). Trigger: a task-1 student committed Saturday morning before the clone and was told "0 commits". `nudges.py` now preserves posted flags on regeneration and `feedback.py --edit` corrects an already posted issue. Identity note: the repo owner is the student regardless of git author name (17 task-1 students used a second identity; all counted). | report from a task-1 student |
| IMP-22 | baseline repair | done 2026-09-05 | Two 2025 students (usernames containing "ric") were filtered as teachers by the substring rule during tasks 1-6 and stored with 0 commits; their commits were re-extracted from the clones into `data/2025/task-{1..6}/commits.csv` (as-delivered effort/nudges untouched). Audit of the rest: no cross-student commits, no non-owner issue creators in 2026; 134 of 160 task-1 students commit under a display name and are counted via the repo owner. | user question on identities |
| IMP-23 | analysis | done 2026-09-05 | Report scaffold in `report/` (ACM `acmart`, nonacm working-report style) with generated tables and figures: `history.py` and `compare.py` write LaTeX tables, new `behaviour.py` profiles commit shape, message quality, issue fidelity and pacing per cohort (2020–2026), `report.py` regenerates everything and builds the PDF. | user: report confirming 2025 vs history plus behavioural changes |
| IMP-24 | tooling | done 2026-09-12 | Public/private split. The GitHub remote is public and receives only the tooling: `publish.py` copies an allowlist (scripts, README, guide, runbook, study and improvements docs, cohort configs without the `teachers` names) onto the `public` branch and refuses to commit if a student username or a CSV would be included. `main` with `data/`, `students/`, `feedback/`, `reports/` stays local. | a TA wants to try the tooling |
| IMP-25 | treatment | done 2026-09-12 (before task-2 posting) | Wording only, v2 messages: singular counts ("1 commit", "1 issue") in the coding line, and the unchanged-category line no longer repeats itself ("as last week (same footing as last week)" becomes "as last week."). Scores and categories untouched; v1 text unchanged. First affected message: task-2 2026 (task-1 had no previous week). | task-2 dry run review |
| IMP-26 | treatment | done 2026-09-12 (before task-2 posting) | "Where you are" class sentence rewritten so the three numbers read as one picture instead of three buckets: "Class this week: 56% are Workflow Master and another 19% Strong Practitioner, so 74% are at that level or above. 7% are still Getting Started." The cumulative share is now computed from counts (rounding the two shares first gave 75% for 74.1%). Same sentence in the first-week, unchanged and moved variants; the Master streak line is unchanged. | user review of the task-2 dry run |
| IMP-27 | tooling (+ treatment wording for a separate cohort only) | done 2026-09-12 | Support for a separately managed group without the pre-filled issue links (plus track). Cohorts are keyed by config file name (`2026-plus`, with `year` in the JSON); `data/`, `repos/`, `students/` follow the name; `ice.py` skips task mapping, comparison and history for a named cohort unless `--baseline` is given. `default_plan: false` makes `plan.py` treat every issue as the student's own and switches the v2 planning line to link-free wording; the v1 model is replaced by v2 for such a cohort; a one-part task (`exercises: 1`) is fully planned by one issue (breadth rule now requires more than one part). `discover.py` keeps the manual exercise count (else 1) when a README has no exercise headings. Regression: all 2026 and 2025 outputs regenerate byte-identically; `cohorts/example-plus.json` is the template. | plus-track TA wants to try ICE |
